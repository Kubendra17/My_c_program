#include <stdio.h>

int main() {
    int a = 5;      // assignment: a is assigned the value 5
    int b = 5;

    if (a == b) {   // comparison: checks if a is equal to b
        printf("a is equal to b\n");
    } else {
        printf("a is not equal to b\n");
    }

    return 0;
}
